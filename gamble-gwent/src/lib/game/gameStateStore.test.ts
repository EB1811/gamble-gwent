import {describe, it, afterEach, expect, vi} from 'vitest'

describe('getFullRandomHand', () => {
  afterEach(() => {
    vi.restoreAllMocks()
  })

  it(`test`, () => {
    expect('2').toEqual('2')
  })
})
